version: '3.9'
services:
  client:
    build: ./client
    ports:
      - "5173:5173"
    env_file: ./client/.env.local
    depends_on:
      - server

  server:
    build: ./server
    ports:
      - "4000:4000"
    env_file: ./server/.env
